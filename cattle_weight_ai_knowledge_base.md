---
tags:
  - ai
  - computer-vision
  - yolo
  - nextjs
  - prisma
  - knowledge-base
---

# 🐄 Smart CattleWeight AI: Knowledge Base

บันทึกความรู้นี้สรุปแนวทางการพัฒนาระบบ AI เพื่อทำนายน้ำหนักโค และโครงสร้างสถาปัตยกรรมของเว็บแอปพลิเคชันปัจจุบัน เพื่อให้สามารถนำไปพัฒนาต่อยอดได้อย่างเป็นระบบ

---

## 🤖 1. แนวทางการพัฒนา AI ทำนายน้ำหนักโค (2 ขั้นตอน)

การทำนายน้ำหนักจากภาพถ่ายโค สามารถแบ่งออกเป็น 2 ขั้นตอน (Pipeline) อย่างมีประสิทธิภาพดังนี้:

### ขั้นตอนที่ 1: การแบ่งส่วนภาพ (Image Segmentation)
**เป้าหมาย:** แยกตัวโคออกจากภาพพื้นหลังให้แม่นยำ เพื่อสกัดข้อมูลรูปร่างและขนาด
*   **กระบวนการ:** นำภาพถ่ายเข้าสู่โมเดล Instance Segmentation โมเดลจะสแกนหาวัตถุที่เป็น "โค" และสร้างพิกัดกรอบ (Bounding Box) พร้อมกับสร้างหน้ากาก (Mask) คลุมเฉพาะส่วนที่เป็นตัวโค
*   **ผลลัพธ์ (Output):**
    1.  ภาพที่ครอบ (Crop) และตัดพื้นหลังออกแล้ว (Background Removal)
    2.  **พื้นที่พิกเซล (Pixel Area):** คำนวณจากจำนวนพิกเซลทั้งหมดที่อยู่ใน Mask ของตัวโค (ซึ่งมีความสัมพันธ์โดยตรงกับขนาดและน้ำหนักของโค)

### ขั้นตอนที่ 2: การทำนายน้ำหนัก (Weight Regression)
**เป้าหมาย:** นำข้อมูลคุณลักษณะที่ได้สกัดมา ไปคำนวณหาน้ำหนักจริง (กิโลกรัม)
*   **กระบวนการ:** แม้เทคนิคตระกูล YOLO จะถูกออกแบบมาเพื่อหาพิกัดและแยกวัตถุ แต่เราสามารถนำข้อมูลภาพที่ถูกสกัดในขั้นแรกมารวมกับโมเดลแบบ **Regression** ได้ โดยมี 2 แนวทางที่นิยม:
    *   **แนวทางที่ 2.1 (CNN Regression):** นำภาพโคที่ไม่มีพื้นหลัง ป้อนเข้าสู่โมเดล CNN (เช่น ResNet, EfficientNet) โดยปรับเลเยอร์สุดท้าย (Fully Connected) ให้ส่งออกค่าเป็นตัวเลข (Regression) และอาจเพิ่มฟีเจอร์ "พื้นที่พิกเซล" เข้าไปในเครือข่ายด้วย
    *   **แนวทางที่ 2.2 (Machine Learning + YOLO):** ไม่ต้องใช้ CNN ทำนายน้ำหนักจากภาพโดยตรง แต่ใช้สัดส่วนเชิงมิติ เช่น ความยาวลำตัว (สกัดจาก Bounding Box) และ พื้นที่พิกเซล (สกัดจาก Mask) นำตัวเลขเหล่านี้ไปเข้าโมเดลคณิตศาสตร์อย่าง Multiple Linear Regression, Random Forest หรือ XGBoost เพื่อหาน้ำหนัก

### 🛠️ เครื่องมือที่แนะนำสำหรับการเทรน (Training Tools)
*   **เตรียมข้อมูล (Data Preparation):** [Roboflow](https://roboflow.com/) - ใช้สำหรับ Label ภาพ, วาด Polygon รอบตัวโค และทำ Data Augmentation (เพิ่มจำนวนรูปภาพด้วยการพลิกซ้ายขวา ปรับแสง)
*   **เฟรมเวิร์กโมเดล (Model Framework):**
    *   **ขั้นตอนที่ 1:** ใช้ **Ultralytics (YOLOv8-seg หรือ YOLO11-seg)** - เป็นโมเดลที่ใช้งานง่ายที่สุดในปัจจุบัน มีประสิทธิภาพสูงและทำงานได้เร็ว
    *   **ขั้นตอนที่ 2:** ใช้ **PyTorch** หรือ **TensorFlow** (สำหรับสร้าง CNN Regression) หรือใช้ **Scikit-Learn** / **XGBoost** (สำหรับเทรนด้วยตัวเลขพื้นที่)
*   **สภาพแวดล้อมรันไทม์ (Environment):** Google Colab หรือ Kaggle (ให้บริการ GPU ฟรีเหมาะสำหรับการเริ่มต้น Train โมเดล)

---

## 💻 2. โครงสร้างของโปรเจกต์เว็บไซต์ปัจจุบัน (Web Project Architecture)

ระบบปัจจุบันพัฒนาด้วยเทคโนโลยี Modern Web Stack โดยมุ่งเน้นความรวดเร็วและรองรับการใช้งานบนหน้าจอมือถือ (Mobile-Responsive)

### 📌 เทคโนโลยีและภาษาหลัก (Tech Stack)
*   **Framework หลัก:** **Next.js (App Router)** - เฟรมเวิร์กของ React ที่รองรับ Server-Side Rendering (SSR)
*   **ภาษาโปรแกรม (Language):** **TypeScript** (ทำงานร่วมกับ React) ช่วยจัดการชนิดตัวแปรให้เข้มงวด ลดข้อผิดพลาดในโค้ด
*   **ดีไซน์และ UI:** **Tailwind CSS** (เขียนสไตล์แบบ Utility-class), **Lucide React** (สำหรับไอคอน) และ **Recharts** (สำหรับสร้างกราฟประวัติ)
*   **การประมวลผลเซิร์ฟเวอร์ (Hosting):** **Vercel**

### 🗄️ ฐานข้อมูล (Database)
*   **ระบบฐานข้อมูล (RDBMS):** **PostgreSQL** (ใช้งานผ่าน Cloud ของ Supabase)
*   **ตัวกลางเชื่อมต่อฐานข้อมูล (ORM):** **Prisma** - เป็นเครื่องมือที่ใช้เขียนโครงสร้างตารางและดึงข้อมูลด้วยภาษา TypeScript ทำให้ไม่ต้องเขียน SQL แบบดิบๆ

### 📁 โครงสร้างไฟล์ที่สำคัญ (Project Structure)
```text
SmartCattleWeightApp/web/
├── prisma/
│   └── schema.prisma        # หัวใจหลักของ Database กำหนดตาราง Animal และ Measurement
├── src/
│   ├── app/                 # ไดเรกทอรีเก็บหน้าเว็บทั้งหมด (Routing)
│   │   ├── page.tsx         # หน้าแรก (Login / Authentication จำลอง)
│   │   ├── dashboard/       # หน้าแรกเมื่อเข้าสู่ระบบ (สรุปภาพรวม)
│   │   ├── animals/         # หน้าแสดงรายชื่อสัตว์ในฟาร์ม
│   │   │   └── [id]/        # หน้าแสดงรายละเอียดของสัตว์แต่ละตัว (Dynamic Route)
│   │   ├── scan/            # หน้าสำหรับถ่ายรูป/อัปโหลด เพื่อทำนายน้ำหนักใหม่
│   │   ├── history/         # หน้าแสดงประวัติการทำนายทั้งหมดเรียงตามเวลา
│   │   └── medicine/        # หน้าคำนวณปริมาณยาและประวัติวัคซีน
│   └── ...
├── package.json             # จัดการไลบรารีต่างๆ ที่โปรเจกต์ใช้งาน
└── tailwind.config.js       # ตั้งค่าสี ฟอนต์ และการแสดงผลของ Tailwind
```

### 🔄 การไหลของข้อมูล (Data Flow) ในระบบปัจจุบัน
1.  **ตาราง `Animal`**: เก็บข้อมูลพื้นฐาน เช่น รหัส, ชื่อ, ภาพโปรไฟล์
2.  **ตาราง `Measurement`**: เมื่อมีการกดเพิ่มการสแกนใหม่ ระบบจะบันทึกน้ำหนักส่วนสูง (ทั้ง AI และที่วัดจริง) ลงในตารางนี้ โดยเชื่อมโยงกับตัวโค (Animal)
3.  **ระบบ LocalStorage**: ในฟังก์ชันยาและวัคซีน (`/medicine`) ข้อมูลยังถูกเก็บจำลองไว้บนเบราว์เซอร์ชั่วคราว (เพื่อรอการขยายฐานข้อมูลในอนาคต)
